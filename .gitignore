.venv/
datasets/raw/
*__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
.env
.claude/
docs/superpowers/
docs/po/
CLAUDE.md
plan.md
.pytest_cache/
logs/
.worktrees/
# Unpacked OFF dump (huge)
datasets/raw/en.openfoodfacts.org.products.csv
datasets/raw/en.openfoodfacts.org.products.parquet

# manual labeling app creates .bak files on every save
datasets/manual_label/*.bak
datasets/manual_label/off_cache/

# P2-EXP10: downloaded product images (~200-400MB, too heavy to commit)
datasets/raw/off_images/
datasets/manual_label/off_cache*/*

# Огромные промежуточные артефакты (>100 MB), которые GitHub не примет
datasets/processed/all_products.parquet
# Скомпилированный бинарь gateway-демо
demo/gateway/gateway

# Локальная конфигурация latexmk — у каждого свои абсолютные пути.
**/.latexmkrc

# LaTeX-артефакты сборки ВКР и презентации.
**/*.aux
**/*.bbl
**/*.bcf
**/*.blg
**/*.fdb_latexmk
**/*.fls
**/*.glg
**/*.glo
**/*.gls
**/*.glsdefs
**/*.glo-abr
**/*.ist
**/*.log
**/*.out
**/*.run.xml
**/*.synctex.gz
**/*.toc
**/*.acn
**/*.acr
**/*.alg
**/*.lof
**/*.lot
**/*.xdv
**/*.xdy
**/*.nav
**/*.snm
**/*.vrb

# MPNet embedding caches (regeneratable, ~147 MB total — see src/eval/method_comparison.py)
datasets/processed/*_v4_embeddings.npy

scans/